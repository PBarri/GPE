package com.awg.gpe.web.test;

import java.time.LocalDate;
import java.time.LocalDateTime;

import org.junit.Ignore;
import org.junit.Test;

import com.awg.gpe.data.model.TGpeMTask;

/**
 * @author Pablo Barrientos
 * @since 1.0
 *
 */
@Ignore
public class TaskFlatMapTest {
	/*
	private Integer psiWeight = 1;
    private Integer evWeight = 1;
    private Integer gpiWeight = 1;
    private Integer asiWeight = 3;
    private Integer dsiWeight = 4;
    private Integer csiWeight = 5;
    private Integer iasWeight = 1;	
	
    @Test
    @Ignore
	public void testWeights() {
		Float psiPercentage = getNormalizedWeight(psiWeight);
		Float evPercentage = getNormalizedWeight(evWeight);
		Float gpiPercentage = getNormalizedWeight(gpiWeight);
		Float asiPercentage = getNormalizedWeight(asiWeight);
		Float dsiPercentage = getNormalizedWeight(dsiWeight);
		Float csiPercentage = getNormalizedWeight(csiWeight);
		Float iasPercentage = getNormalizedWeight(iasWeight);
		
		System.out.println(String.format("PSI Percentage: %s", psiPercentage));
		System.out.println(String.format("EV Percentage: %s", evPercentage));
		System.out.println(String.format("GPI Percentage: %s", gpiPercentage));
		System.out.println(String.format("ASI Percentage: %s", asiPercentage));
		System.out.println(String.format("DSI Percentage: %s", dsiPercentage));
		System.out.println(String.format("CSI Percentage: %s", csiPercentage));
		System.out.println(String.format("IAS Percentage: %s", iasPercentage));
		
		System.out.println(String.format("Suma Total: %s", psiPercentage + evPercentage + gpiPercentage + asiPercentage + dsiPercentage + csiPercentage + iasPercentage));
	}
    	
	private Float getNormalizedWeight(Integer weight) {
		return (new Float(weight * 100)) / (gpiWeight + psiWeight + evWeight + asiWeight + dsiWeight + csiWeight + iasWeight);
	}
	*/
}
